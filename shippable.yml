language: node_js

node_js:
  - 10.24

build:
  ci:
    - mkdir -p test
    - touch test/test1.txt
    - docker run -v $SHIPPABLE_BUILD_DIR/test:$SHIPPABLE_BUILD_DIR/test ubuntu "ls $SHIPPABLE_BUILD_DIR/test"
