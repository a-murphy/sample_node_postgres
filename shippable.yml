language: node_js

jdk:
   - oraclejdk8
   - openjdk7
   - openjdk6
   
services:
  - postgres

build:
  pre_ci_boot:
    image_name: drydock/u14jav
    image_tag: prod
    pull: true
  ci:
    - echo $JAVA_HOME
    - java -version
    - npm install
    - npm test
